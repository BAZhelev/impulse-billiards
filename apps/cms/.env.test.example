# Test environment — copy to .env.test (local e2e/int tests use the `cms_test` database)
PORT=3001
DATABASE_URL=postgres://postgres:postgres@127.0.0.1:5432/cms_test
PAYLOAD_SECRET=local-test-secret-not-for-production